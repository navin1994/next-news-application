export default function NewsNotFoundPage() {
    return (
        <div id="error">
            <h1>Not Found!</h1>
            <p>Unfortunately, We could not find requested news!</p>
        </div>
    );
}